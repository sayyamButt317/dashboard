export interface Product {
  _id: string;
  productName: string;
  productDescription: string;
  price: number;
  category: string;
  stock: number;
   // discountType: string;
  // discountPercent: number;
  // status: string;
  // gender: string;
  // size:string;
  picture:string;
  createdAt:string
}
